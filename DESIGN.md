# DESIGN.md — Filmora WebUI 设计系统

> 依据 HaizhucodexDesignSkill 工作流产出：设计简报 → 视觉论点 → Tokens → 组件规则 → 动效 → 无障碍 → 验证。

## 1. 设计简报
- **产品**：Filmora WebUI — AI 视频工作台（管理员单用户工作工具）
- **受众**：内容创作者 / 视频操作员；高频、专注、长时间使用
- **主任务**：上传素材 → 剪辑/滤镜/导出 → AI 生成与修改 → 管理 API 密钥
- **品牌属性**：专业剪辑（Filmora/Wondershare）· 电影工业感 · 可靠 · 高效
- **平台**：桌面优先 Web，1440px 基准，适配 1024+ 平板与窄屏
- **约束**：中文界面，暗色长期使用舒适，数据密集但层级清晰

## 2. 视觉论点（Visual Thesis）
**"Cinema Control Room"（电影控制室）**
- 近黑环境（`#0A0B0E`）承载素材与视频 —— 让画面成为焦点
- 单一暖色强调：**胶片金**（`#F5B841`）用于主行动、选中、进度
- 信息层用中性灰阶区分，数据一律等宽字体（JetBrains Mono）
- 细网格背景 + 微弱噪点纹理，营造"调色间/控制台"质感
- 拒绝：大面积渐变、玻璃拟态泛滥、彩色卡片堆砌、无意义动画

## 3. 设计 Tokens
### 颜色
| Token | 值 | 用途 |
|---|---|---|
| ink-0 | `#0A0B0E` | 页面底 |
| ink-1 | `#101216` | 面板 |
| ink-2 | `#171A20` | 抬升面板 |
| ink-3 | `#1E222B` | hover 面 |
| line | `#23262E` | 边框/分隔 |
| txt-1 | `#F2F4F8` | 主文本 |
| txt-2 | `#A6ADBB` | 次级文本 |
| txt-3 | `#6B7280` | 弱化/标签 |
| gold-400 | `#F5B841` | 主强调/CTA/选中 |
| mint / ember / sky2 | `#6EE7B7` `#F87171` `#7DD3FC` | 成功/危险/进行中 |

### 字体
- UI：Inter + Noto Sans SC（500/600/700 阶梯）
- 数据：JetBrains Mono（时长、尺寸、job id、key）

### 圆角 / 阴影
- 面板 `rounded-xl` + 单层微阴影（`0 8px 30px rgba(0,0,0,.35)` + 1px 内高光）
- 控件 `rounded-lg`；chip `rounded-full`
- 强调光晕：`0 0 0 1px rgba(245,184,65,.25) 0 0 24px rgba(245,184,65,.12)`

### 间距密度
- 页面 `p-6/8`，面板间距 `gap-6`，紧凑表格行高 `py-3`

## 4. 组件规则
- **按钮**：`primary`（金底黑字，带投影）、`outline`、`ghost`、`danger`；统一 32px 高、圆角 8px；禁用 40% 透明度 + 禁止点击
- **输入**：ink-0 底、细边框、聚焦时金色边框+光晕；`label` 全部大写字母间距 0.14em 的小标签
- **卡片（素材）**：视频缩略图 hover 轻微放大 + 暗色遮罩 + 播放按钮浮现；右下角时长 mono chip
- **状态**：chip 系统（成功=绿、失败=红、运行=蓝、排队=灰）用于任务/密钥
- **空状态**：虚线框图标 + 主文案 + 提示文案
- **表格**：`divide-y` 细分割线，首行大写微标签表头，mono 数据列

## 5. 动效计划
- 页面/面板进入：`fadeUp`（0.45s cubic-bezier(.22,1,.36,1)），卡片列表按 40ms stagger
- 进度条：宽度过渡 300ms
- 状态点：`pulseSoft` 2.4s 呼吸（仅用于"运行中/在线"语义）
- 按钮按下 `active:scale-[.98]`
- **克制原则**：动效只服务于反馈/层级/方向，禁止全局装饰性动画
- `prefers-reduced-motion` 兼容：动画仅 opacity/transform 且短时

## 6. 无障碍
- 语义 HTML：`nav aria-label`、`role=toolbar/tab`、`role=alert`
- 键盘导航：全部按钮/链接可达，焦点环金色
- 对比度：文本对 ink-0 ≥ 8:1；弱化文本 ≥ 4.5:1
- 触控目标 ≥ 32px；表单 label 关联
- 语言 `zh-CN`

## 7. 验证
- Playwright 全页截图（登录/素材/剪辑/AI/密钥）无 console error
- 像素统计：暗色占比 92%+、金色强调像素存在、页面非空白
- 响应式：1440px 主布局、1024 下网格降列
